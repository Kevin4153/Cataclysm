const parseChatGPTResponse = (response) => {
    // Implement parsing logic here based on the response format
    // Example structure:
    // {
    //   ingredient: "chocolate",
    //   toxicityLevel: 3,
    //   symptoms: ["vomiting", "diarrhea"]
    // }
    const parsedResponse = {
      // Extract and format data from the response
    };
  
    return parsedResponse;
  };
  
  module.exports = { parseChatGPTResponse };
  